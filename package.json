{
  "name": "sohn-compositions-portfolio",
  "version": "1.0.0",
  "description": "Portfolio website for Joshua Sohn",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon",
    "start": "node .",
    "heroku-postbuild": "cd ./client && npm install"
  },
  "author": "Ben Turner",
  "license": "ISC",
  "dependencies": {
    "@sendgrid/mail": "^6.5.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  },
  "engines": {
    "node": "10.16.0"
  },
  "nodemonConfig": {
    "ignore": [
      "client/*"
    ]
  }
}